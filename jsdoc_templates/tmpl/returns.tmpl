<?js
var data = obj || {};
if (data.description) {
?>
<?js } ?>

	<ul class="params"><li>
	    <span>
	    	<?js if (data.type && data.type.names) {?>
	        <?js= this.partial('type.tmpl', data.type.names) ?>
			<?js } ?>
	    </span>
		<?js
		var data = obj || {};
		if (data.description) {
		?>
		<div class="param-desc">
		    <?js= description ?>
		</div>
		<?js } ?>
	</li></ul>
